defaultNamespace: cert-manager
helm:
  chart: https://github.com/estenrye/fleet/raw/main/charts/cert-manager-v1.3.1.tgz
targetCustomizations:
- helm:
    values:
      prometheus:
        enabled: true
        servicemonitor:
          enabled: false
    clusterSelector: {}