defaultNamespace: cert-manager
helm:
  chart: cert-manager
  repo: jetstack
  releaseName: cert-manager
  version: 1.3.1
targetCustomizations:
- helm:
    values:
      installCRDs: false
      prometheus:
        enabled: true
        servicemonitor:
          enabled: false
    clusterSelector: {}